{
  "name": "runspec",
  "version": "0.1.0",
  "description": "Executable enterprise application builder for production-ready systems.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit false",
    "verify:markdown": "npm run build && node dist/src/cli.js verify-markdown",
    "verify:blueprint": "npm run build && node dist/src/cli.js verify-blueprint",
    "blueprint:print": "npm run build && node dist/src/cli.js blueprint-print",
    "agent:next": "npm run build && node dist/src/cli.js agent-next",
    "test": "npm run build && node --test dist/test/*.test.js && node dist/src/cli.js verify-markdown && node dist/src/cli.js verify-blueprint"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.0",
    "typescript": "^5.9.3"
  }
}
